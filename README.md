# README
## mpython - simple MicroPython examples and setup
## Setup Files
* config.py - configuration file for mpremote utility. Save file at `~/.config/mpremote/config.py` then use mpremote to run commands.
## MicroPython Examples
* blink_key - will blink builtin LED until a key press is detected on the serial port
